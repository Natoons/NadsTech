@* Composant HeatmapStatsSidebar (mock) *@
<div class="bg-white dark:bg-gray-800 rounded-lg shadow p-4">
    <h3 class="text-lg font-semibold mb-2 text-gray-900 dark:text-white">🔥 Heatmap des articles populaires (semaine)</h3>
    <div class="grid grid-cols-7 gap-1 mb-2">
        @foreach (var day in Days)
        {
            <div class="text-xs text-center text-gray-500">@day</div>
        }
        @foreach (var val in Heatmap)
        {
            <div class="rounded flex items-center justify-center" style="height:32px;width:32px;background:@GetColor(val)">
                <span class="text-xs font-bold text-white">@val</span>
            </div>
        }
    </div>
    <div class="text-xs text-gray-500">(Démo : stats factices, couleurs = popularité)</div>
</div>

@code {
    // Mock : 7 jours, 7 valeurs
    private string[] Days = new[] { "Lun", "Mar", "Mer", "Jeu", "Ven", "Sam", "Dim" };
    private int[] Heatmap = new[] { 2, 8, 5, 12, 20, 7, 1 };

    private string GetColor(int value)
    {
        // Bleu clair (peu populaire) à rouge foncé (très populaire)
        if (value >= 15) return "#b91c1c"; // rouge foncé
        if (value >= 10) return "#ef4444"; // rouge
        if (value >= 7) return "#f59e42"; // orange
        if (value >= 4) return "#fbbf24"; // jaune
        if (value >= 2) return "#60a5fa"; // bleu
        return "#dbeafe"; // bleu très clair
    }
} 